{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:8081",
          "https://travel-expenses-app.expo.app"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["Content-Type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
